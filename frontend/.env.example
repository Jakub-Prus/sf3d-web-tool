# Optional browser-side override when the frontend should call a backend directly.
# Leave unset to use the built-in same-origin /api proxy.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Server-side proxy target used by the frontend /api route handler.
BACKEND_PROXY_URL=http://127.0.0.1:8000/api
